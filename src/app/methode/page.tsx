import type { Metadata } from "next";
import MethodePage from "@/components/pages/MethodePage";

export const metadata: Metadata = { title: "Méthode SIGNAL" };

export default function Page() {
  return <MethodePage />;
}
