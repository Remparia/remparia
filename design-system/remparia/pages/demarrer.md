# Services & Démarrer

> Overrides MASTER for `/services`, `/services/[slug]`, `/demarrer`.

- Shell: `PremiumPageShell` + `PremiumHero` + `PremiumSection` + `PremiumCtaBand`
- Demarrer paths: `PremiumCard` inside `ph-workforce` (no `info-card`)
- Pricing: fixed-fee model before start; no invented € amounts; no “grid TBD” copy
- “Et ensuite”: multi-workflows → AgentOps (short definition) → autonomy; link to OS deploy offers; anchor `#agentops`
- Service hub: delivery layer (mise en œuvre), not a parallel product to the OS
- Service detail: promise + deliverable + platform link + checklist; CTA `/demarrer`
- CTA href demarrer default; contact for demarrer page
