# Cas d’usage — page override

> Overrides `design-system/remparia/MASTER.md` for `/cas-d-usage`.

## Intent

Before/after process cards (today vs agent) with trust constraints (never / measure).

## Rules

- Numbered tags `01`, `02`… — no emoji
- Meta rows use `.signal-card__meta` (dt accent, dd muted ≥ 0.82 contrast)
- Closing band: one primary CTA + one secondary to Solution
- Cards are informational (`cursor: default`)
