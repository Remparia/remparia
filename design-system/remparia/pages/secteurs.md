# Secteurs pages

> Overrides MASTER for `/secteurs` and `/secteurs/[slug]`.

- Shell: `PremiumPageShell` + `PremiumHero` / `SecteurHero` (media split) + `PremiumSection` + `PremiumCtaBand`
- Detail: alternate dark/light sections; agent fiches on light band
- Hub: filters stay `.secteur-filters` inside `PremiumSection`
- CTA: lime `PremiumCtaBand` only
