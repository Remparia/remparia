# Secondary marketing pages

> Overrides for `/cas-d-usage`, `/pour-qui`, `/a-propos`, `/contact`, `/carrieres`.

- Shell: `PremiumPageShell` + `PremiumHero` + `PremiumSection` (+ `PremiumCtaBand` where relevant)
- Cards: `PremiumCard` / `ph-workforce` instead of `info-card`
- Contact form kept; wrapped in light `PremiumSection`
- Careers: same shell; role cards as `PremiumCard`
