# Pour qui — page override

> Overrides `design-system/remparia/MASTER.md` for `/pour-qui`.

## Intent

Audience segmentation (families) + clear “not for” list. Path selection pattern from Enterprise Gateway.

## Rules

- Family cards: tag + title + short desc + text links only (no card-as-button unless linked)
- “Not for” via `.proof-badges` rail list
- Dual CTA after exclusion list: primary Démarrer, ghost Cas d’usage
- Keep one purpose per section; no hero stats
