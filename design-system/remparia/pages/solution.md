# Solution — page override

> Overrides `design-system/remparia/MASTER.md` for `/solution`.

## Intent

Trust & Authority: capabilities, autonomy heatmap, security, deployment — enterprise gateway depth.

## Rules

- No emoji status icons — use `.status-dot` (square) + `.heatmap-card--{green|amber|red}`
- Hero: brand-locked PageHero; solid accent eyebrow (no text-clip blur)
- Cards: static `article.info-card` (cursor default); CTAs via `.btn-primary` / `.btn-ghost`
- Proof lists: hairline border + left accent rail (`.proof-badges__item`)
- One job per section; no extra promo chips in hero
